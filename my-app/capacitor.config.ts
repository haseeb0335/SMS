import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.haseeb.schoolmanagement',
  appName: 'School Management System',
  webDir: 'build',
 
};

export default config;